# 服务监听端口
PORT=3000

# 扫描二维码后要跳转到的真实链接
TARGET_URL=https://github.com/Seanoll/QRcode-Refresh

# 二维码刷新间隔（毫秒），默认 10000 即 10 秒
REFRESH_INTERVAL_MS=10000

# 二维码内容的公网访问基础地址（必须能被扫码设备访问到本服务）
# 本地开发用 http://localhost:3000；部署后改成实际域名
PUBLIC_BASE_URL=http://localhost:3000
